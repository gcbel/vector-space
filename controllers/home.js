/* DEPENDECIES */
const router = require('express').Router();
const withAuth = require('../utils/auth.js')  // Import authentication middleware
const { } = require('../models');             // Import models

/* ROUTES */

/* EXPORTS */
module.exports = router;